API_KEY=sk-...
MODEL=gpt-4o
# openssl rand -hex 32 로 생성해서 배포 환경마다 다른 값으로 교체할 것
JWT_SECRET=change-me-in-production
UPLOAD_DIR=./data/uploads
# 배포 시 프론트 도메인으로 채우기 (콤마로 여러 개 가능). 비워두면 전부 허용(개발용).
CORS_ORIGINS=
