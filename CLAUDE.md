# 하루결 (Harugyeol)

## 프로젝트 개요

하루결은 교대근무자(간호사, 소방관, 공장 근로자 등)를 위한 근무 루틴 관리 앱입니다.

핵심 기능:

1. **근무표 인식** — 사용자가 근무표 사진을 업로드하면 AI가 인식해 일정으로 등록
2. **맞춤 루틴 안내** — 근무 유형(주간/야간/비번 등)에 맞춘 일일 루틴 제안 (수면, 식사, 카페인 섭취 등)
3. **이상 징후 대응** — 심박수 급상승, 졸음 위험 등 신체 이상 징후 발생 시 대처법 추천 및 근무 일정 재조정

## 기술 스택

### Backend

- **FastAPI** (Python) — AI/LLM 생태계 활용, 보일러플레이트 최소화, Swagger 자동 생성
- **SQLite + SQLAlchemy** — 별도 DB 컨테이너 없이 파일 기반으로 운영. 필요 시 연결 문자열 교체만으로 PostgreSQL 전환 가능
- **Docker** — 로컬 개발 및 EC2 배포 모두 컨테이너로 실행. `app.db`는 volume에 마운트해 영속성 확보

### 배포

- EC2 단일 인스턴스 + Docker

## 구조

```
backend/    서버 (API, AI 처리)
frontend/   클라이언트 (앱 UI)
```

## 규칙

커밋 메시지, 브랜치, PR 규칙은 [CONTRIBUTING.md](./CONTRIBUTING.md)를 따릅니다. 커밋이나 PR 생성 시 해당 문서의 컨벤션을 확인하고 작성해 주세요.
